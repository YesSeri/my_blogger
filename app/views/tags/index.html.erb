<h1> ALL TAGS</h1>

<ul>
  <% @tags.each do |tag| %>
    <li> <%= link_to tag.name, tag_path(tag) %> <%= link_to "_______________delete", article_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %></li>
  <% end %>
</ul>
<p>
<%= link_to 'Articles', articles_path%>
</p>
